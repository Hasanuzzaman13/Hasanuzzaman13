#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	
    int n,a,b,c,d,e,sum;
    scanf("%d", &n);
   a=n%10;
   b=(n%100)/10;
   c=(n%1000)/100;
   d=(n%10000)/1000;
   e=n/10000;
   sum=a+b+c+d+e;
   printf("%d",sum);
    return 0;
}
